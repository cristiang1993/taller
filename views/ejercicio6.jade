extends layoutejercicio6

block content
  div.col-md-6.col-md-offset-3.container
        div(ng-controller="TiendaController")
          div.well(ng-repeat="b in articulo1")
           h1 {{b.nombre }}
           h3 Precio: {{b.precio | currency:"€":2 }}
           h3 stock: {{b.stock }}
           IMG.img-responsive.container(ng-src="{{b.imagen }}")

     

